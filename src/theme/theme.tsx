import { createMuiTheme } from '@material-ui/core/styles';
const theme = createMuiTheme({
  palette: {
    primary: {
      main: "#ffffff",
      // light: "#fafafa",
      light: "#f4f5f7",
    },
    secondary: {
      main: '#2962FF',
    }
  },
});


export default theme;